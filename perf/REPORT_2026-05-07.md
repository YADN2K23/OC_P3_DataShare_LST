# Rapport performance - 2026-05-07

## Contexte

- Date: 2026-05-07
- Outil: k6 v1.7.1
- Scripts versionnes:
  - `perf/login_k6.js`
  - `perf/files_k6.js`
- Cible prevue: `http://localhost:8080`

## Verification prealable

Commande executee:

```powershell
k6 version
```

Resultat:

```text
k6.exe v1.7.1 (commit/9f82e6f1fc, go1.26.1, windows/amd64)
```

Sonde backend:

```powershell
Invoke-WebRequest -Uri http://localhost:8080/api/me -Method GET -TimeoutSec 3
```

Resultat: timeout local apres 3 secondes. Le backend n'etait pas disponible de facon exploitable pendant cette session.

## Conclusion

Le benchmark de charge n'a pas ete execute sur une cible applicative disponible. Les scripts k6 sont maintenant versionnes pour la soutenance et doivent etre relances des que le backend est demarre avec un compte de test valide.

## Commandes a relancer

```powershell
$env:BASE_URL="http://localhost:8080"
$env:LOGIN_USER="demo"
$env:LOGIN_PASSWORD="demo123"
$env:VUS="20"
$env:DURATION="1m"
k6 run .\perf\login_k6.js
k6 run .\perf\files_k6.js
```

## Budget cible

- `POST /api/login`: p95 < 300 ms, p99 < 600 ms, erreurs < 1%.
- `/api/files` liste/upload/download: p95 < 500 ms, p99 < 1000 ms, erreurs < 1%.
