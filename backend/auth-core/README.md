# auth-core

Module reutilisable qui centralise la logique JWT.

## Build

```powershell
mvn -f backend-platform/auth-core/pom.xml clean test
```

## Publication locale

```powershell
mvn -f backend-platform/auth-core/pom.xml clean install
```

